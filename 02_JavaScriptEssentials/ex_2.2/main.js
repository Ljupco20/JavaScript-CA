const name = "Ljupco";
const age = 36;
const bool = true;



console.log(`Hello my name is ${name}, I am ${age} years old and I can code JavaScript: ${bool}` );

let a = prompt();
console.log(a);

let b = prompt();
console.log(b);

let square = (a**2 + b**2);


let c = Math.sqrt(square);

console.log(`Value of a is ${a} and value of b is ${b}, the addition of the two square number is ${square} and the root number is ${c}`);
